# Optional fallback if you prefer env over the settings page.
# The web UI「大模型配置」saves to data/llm-config.json (gitignored).

DEEPSEEK_API_KEY=
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# DEEPSEEK_MODEL=deepseek-chat
